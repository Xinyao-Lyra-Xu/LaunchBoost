# Normalize line endings: LF in the repo, regardless of OS.
# Keeps Prettier (writes LF) and Windows checkouts from fighting over CRLF.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.icns binary
